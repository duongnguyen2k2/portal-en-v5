@model API.Areas.Admin.Models.Banners.BannersModel
@{ 

}

<!---------Banner Right Bottom ---------->
@if (Model.ListItems != null && Model.ListItems.Count() > 0)
{
    @for (int i = 0; i < Model.ListItems.Count(); i++)
    {
        if (Model.ListItems[i].CatId == 7)
        {
            <div class="banner-item">
                <a href="@Model.ListItems[i].Link" target="@Model.ListItems[i].Target">
                    <img src="@Model.ListItems[i].Image" class="d-block w-100" alt="@Model.ListItems[i].Title" />
                </a>
            </div>
        }

    }
}