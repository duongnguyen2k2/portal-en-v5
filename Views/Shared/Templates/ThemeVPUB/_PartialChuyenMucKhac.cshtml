@model API.Areas.Admin.Models.Banners.BannersModel
@{
    List<API.Areas.Admin.Models.Banners.Banners> ListItem2 = new List<API.Areas.Admin.Models.Banners.Banners>();

    @for (int i = 0; i < Model.ListItems.Count(); i++)
    {
        if (Model.ListItems[i].CatId == 4)
        {
            ListItem2.Add(Model.ListItems[i]);
        }

    }
}
@if (ListItem2 != null && ListItem2.Count() > 0)
{
    <div class="banner-lienket-anh">
        <div class="owl-carousel owl-theme carouselChuyenMucKhac">
            @for (int i = 0; i < ListItem2.Count(); i++)
            {
                <div class="item">
                    <a href="@ListItem2[i].Link" target="@ListItem2[i].Target">
                        <img class="img-responsive" style="height:72px;" src="@ListItem2[i].Image" title="@ListItem2[i].Title" />
                    </a>
                </div>
            }

        </div>
    </div>
}


